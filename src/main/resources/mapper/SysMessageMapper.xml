<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.yanque.models.message.mapper.SysMessageMapper">

    <resultMap id="SysMessageMap" type="cn.yanque.models.message.pojo.entity.SysMessageEntity">
        <id property="id" column="id"/>
        <result property="userId" column="user_id"/>
        <result property="title" column="title"/>
        <result property="content" column="content"/>
        <result property="msgType" column="msg_type"/>
        <result property="relatedId" column="related_id"/>
        <result property="isRead" column="is_read"/>
        <result property="createTime" column="create_time"/>
    </resultMap>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO sys_message (user_id, title, content, msg_type, related_id)
        VALUES (#{userId}, #{title}, #{content}, #{msgType}, #{relatedId})
    </insert>

    <select id="selectByUserId" resultMap="SysMessageMap">
        SELECT * FROM sys_message
        WHERE user_id = #{userId}
        <if test="isRead != null">
            AND is_read = #{isRead}
        </if>
        ORDER BY create_time DESC
    </select>

    <select id="countUnread" resultType="int">
        SELECT COUNT(*) FROM sys_message
        WHERE user_id = #{userId} AND is_read = 0
    </select>

    <update id="markAsRead">
        UPDATE sys_message SET is_read = 1 WHERE id = #{id}
    </update>

    <update id="markAllAsRead">
        UPDATE sys_message SET is_read = 1 WHERE user_id = #{userId} AND is_read = 0
    </update>
</mapper>
