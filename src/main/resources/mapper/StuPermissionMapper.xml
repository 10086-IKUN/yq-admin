<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.yanque.models.studentFront.mapper.StuPermissionMapper">

    <resultMap id="StuPermissionMap" type="cn.yanque.models.studentFront.pojo.entity.StuPermissionEntity">
        <id property="id" column="id"/>
        <result property="parentId" column="parent_id"/>
        <result property="permissionCode" column="permission_code"/>
        <result property="permissionName" column="permission_name"/>
        <result property="permissionType" column="permission_type"/>
        <result property="apiPath" column="api_path"/>
        <result property="routePath" column="route_path"/>
        <result property="icon" column="icon"/>
        <result property="sortNum" column="sort_num"/>
        <result property="description" column="description"/>
        <result property="status" column="status"/>
        <result property="createdAt" column="created_at"/>
        <result property="updatedAt" column="updated_at"/>
    </resultMap>

    <select id="selectByStudentId" resultMap="StuPermissionMap">
        select distinct p.*
        from stu_student_permission_template spt
        inner join stu_template_permission tp on tp.template_id = spt.template_id
        inner join stu_permission p on p.id = tp.permission_id
        where spt.student_id = #{studentId}
          and spt.status = 1
          and p.status = 1
          and (spt.effective_start_time is null or spt.effective_start_time &lt;= now())
          and (spt.effective_end_time is null or spt.effective_end_time &gt;= now())
        order by p.sort_num asc, p.id asc
    </select>

</mapper>
