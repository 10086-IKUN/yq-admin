spring:
  data:
    redis:
      host: localhost
      port: 6379
      password: 123456   # 没密码可不写
      database: 0

  datasource:
    url: jdbc:mysql://localhost:3306/yq?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai
    username: root
    password: 123456